name: My workflow

on:
  push:
    branches: [ main ]

jobs:
  send_notification:
    runs-on: ubuntu-latest
    steps:
    - name: Send notification
      env:
        TELEGRAM_BOT_TOKEN: "6222081707:AAHs1oxyC657ZwDtnOxxEVi6qq7VmbWx3VY"
        TELEGRAM_CHAT_ID: "-846727139"
      run: |
        curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
        -d "chat_id=${TELEGRAM_CHAT_ID}" \
        -d "text=Your workflow has completed."
